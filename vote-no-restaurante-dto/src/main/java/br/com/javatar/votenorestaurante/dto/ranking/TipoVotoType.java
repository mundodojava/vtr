package br.com.javatar.votenorestaurante.dto.ranking;

/**
 * The Enum TipoVotoType.
 * 
 * @author ismael
 */
public enum TipoVotoType {

    /** O(a)(s) atendimento. */
    ATENDIMENTO,

    /** O(a)(s) demora. */
    DEMORA,

    /** O(a)(s) sabor. */
    SABOR,

    /** O(a)(s) preco. */
    PRECO,

    /** O(a)(s) localizacao. */
    LOCALIZACAO

}
